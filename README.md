# mazes

## Build
Requires
- C++17 compiler
- CMake
- Boost
- png++
- libpng
- FFmpeg

Build process
```
mkdir build
cmake ..
make
```

Install with `make install`

## Usage
Run `mazes -h` for usage options
